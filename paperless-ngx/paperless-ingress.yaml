apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: paperless-ingress
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`paperless.k8s.zakhaven.uk`)
      services:
        - name: paperless-ngx
          port: 8000